---
phase: 03-experience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/hooks/useAnimationQueue.ts
  - src/hooks/useAnimationQueue.test.ts
  - src/components/Board/GoatPiece.tsx
  - src/components/Board/TigerPiece.tsx
  - src/components/Board/Board.tsx
  - src/components/GameScreen/GameScreen.tsx
  - src/components/GameScreen/GameOverOverlay.tsx
autonomous: true
requirements:
  - POL-01
  - POL-02
  - POL-03
  - POL-04

must_haves:
  truths:
    - "Piece slides animate smoothly between positions (~350ms ease-in-out)"
    - "Tiger capture follows a curved arc path over the goat (~400ms)"
    - "Captured goat fades out after tiger lands (~200ms)"
    - "Chain-hop captures play sequentially with visible pause between hops"
    - "Goat placement has a visible entrance animation"
    - "User input is blocked during animations (no desync)"
    - "Game over triggers winning piece glow before overlay appears"
    - "Sound effects fire in sync with animation events"
  artifacts:
    - path: "src/hooks/useAnimationQueue.ts"
      provides: "Animation queue that processes GameEvents sequentially"
      exports: ["useAnimationQueue", "AnimationState"]
    - path: "src/hooks/useAnimationQueue.test.ts"
      provides: "Tests for animation queue timing and sequencing"
      min_lines: 40
  key_links:
    - from: "src/hooks/useAnimationQueue.ts"
      to: "src/engine/types.ts"
      via: "Consumes GameEvent[] from applyMove results"
      pattern: "GameEvent"
    - from: "src/hooks/useAnimationQueue.ts"
      to: "src/audio/AudioEngine.ts"
      via: "Triggers sound effects for each animation event"
      pattern: "audioEngine\\.play"
    - from: "src/components/GameScreen/GameScreen.tsx"
      to: "src/hooks/useAnimationQueue.ts"
      via: "Passes lastEvents to animation queue, reads isAnimating to gate input"
      pattern: "useAnimationQueue"
    - from: "src/components/Board/GoatPiece.tsx"
      to: "CSS transitions"
      via: "transition property on cx/cy or transform for smooth movement"
      pattern: "transition"
---

<objective>
Add smooth, weighted piece animations to the game board -- slides, capture arcs, chain-hop sequences, placement effects, and game-over celebration glow.

Purpose: Animations transform the game from a functional tool into something that feels like a physical board game. Each piece movement has weight and intent. Chain-hop captures build tension through deliberate sequential hops.

Output: A fully animated game where every move produces a satisfying visual (and audible) response, with user input correctly gated during animation sequences.
</objective>

<execution_context>
@/Users/rk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-experience/03-CONTEXT.md
@.planning/phases/03-experience/03-RESEARCH.md
@.planning/phases/03-experience/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (theme + audio) -->

From src/hooks/useSettings.ts (created in Plan 01):
```typescript
export function useSettings(): {
  theme: 'traditional' | 'modern';
  soundEnabled: boolean;
  setTheme: (t: ThemeName) => void;
  setSoundEnabled: (v: boolean) => void;
}
```

From src/audio/AudioEngine.ts (created in Plan 01):
```typescript
class AudioEngine {
  playPlace(theme: ThemeName): void;
  playSlide(theme: ThemeName): void;
  playCapture(theme: ThemeName, chainIndex?: number): void;
  playWin(theme: ThemeName): void;
  playLoss(theme: ThemeName): void;
  playIllegal(theme: ThemeName): void;
}
export const audioEngine: AudioEngine;
```

From src/engine/types.ts:
```typescript
export type GameEvent =
  | { type: 'PIECE_MOVED'; from: number; to: number; piece: Piece }
  | { type: 'GOAT_CAPTURED'; over: number; landedAt: number }
  | { type: 'GOAT_PLACED'; at: number }
  | { type: 'PHASE_CHANGED'; newPhase: Phase }
  | { type: 'GAME_OVER'; status: GameStatus }
  | { type: 'CHAIN_JUMP_AVAILABLE'; tigerAt: number }
  | { type: 'CHAIN_JUMP_ENDED'; tigerAt: number };
```

From src/engine/board.ts:
```typescript
export const NODES: { id: number; x: number; y: number; adj: number[] }[];
```

From src/hooks/useGame.ts:
```typescript
// Returns lastEvents: GameEvent[] -- the events from the most recent applyMove call
export function useGame(): { ..., lastEvents: GameEvent[], ... }
```

From src/components/Board/GoatPiece.tsx (after Plan 01 -- uses CSS vars):
```typescript
// Uses var(--goat-fill), var(--goat-stroke), var(--select-stroke)
export function GoatPiece({ x, y, isSelected }: GoatPieceProps)
```

From src/components/Board/TigerPiece.tsx (after Plan 01 -- uses CSS vars):
```typescript
// Uses var(--tiger-fill), var(--tiger-stroke), var(--select-stroke)
export function TigerPiece({ x, y, isSelected }: TigerPieceProps)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Animation queue hook and piece component transitions</name>
  <files>
    src/hooks/useAnimationQueue.ts
    src/hooks/useAnimationQueue.test.ts
    src/components/Board/GoatPiece.tsx
    src/components/Board/TigerPiece.tsx
  </files>
  <behavior>
    - useAnimationQueue(lastEvents, soundEnabled, theme) returns { isAnimating, animatingPieces, fadingGoat, placingGoat, gameOverGlow }
    - When lastEvents changes to a non-empty array, isAnimating becomes true
    - Events are processed sequentially: PIECE_MOVED waits 350ms, GOAT_CAPTURED waits 400ms + 200ms fade, GOAT_PLACED waits 250ms
    - Chain-hop: multiple GOAT_CAPTURED events play one at a time with 150ms pause between
    - GAME_OVER event sets gameOverGlow to the winning side for 500ms before completing
    - When queue drains, isAnimating returns to false
    - Sound fires at the start of each event animation (if soundEnabled)
    - GoatPiece accepts optional animationClass and style props for transitions
    - TigerPiece accepts optional animationClass and style props for transitions
    - Pieces have CSS transition on cx/cy (circles) or transform (polygon) for smooth position changes
  </behavior>
  <action>
    1. Create src/hooks/useAnimationQueue.ts:
       - Interface AnimationState: { isAnimating, animatingPieces (Map of nodeId -> {fromX, fromY, toX, toY}), fadingGoat (nodeId | null), placingGoat (nodeId | null), gameOverGlow (GameStatus | null) }
       - The hook receives lastEvents: GameEvent[], soundEnabled: boolean, theme: ThemeName
       - Use useRef to track previously processed events (avoid re-processing same array)
       - On new events: set isAnimating=true, iterate events sequentially using async/await with delay()
       - For PIECE_MOVED: look up NODES[from] and NODES[to] coordinates, set animatingPieces map, play slide sound, await 350ms
       - For GOAT_CAPTURED: set animatingPieces for tiger arc (from -> over -> to), play capture sound with chainIndex, await 400ms for tiger arc, then set fadingGoat, await 200ms for goat fade
       - For GOAT_PLACED: set placingGoat, play place sound, await 250ms
       - For GAME_OVER: play win or loss sound, set gameOverGlow, await 500ms
       - For chain-hops: track chainIndex (increment on each GOAT_CAPTURED), pass to playCapture for escalating pitch
       - Cleanup: return cancelled flag to prevent state updates after unmount
       - Export helper: `function delay(ms: number): Promise<void>`

    2. Create src/hooks/useAnimationQueue.test.ts:
       - Mock AudioEngine (vi.mock('../audio/AudioEngine'))
       - Test: isAnimating is true when events are processing
       - Test: PIECE_MOVED event triggers playSlide and resolves after ~350ms (use vi.useFakeTimers)
       - Test: Multiple GOAT_CAPTURED events process sequentially (not simultaneously)
       - Test: chainIndex increments for consecutive GOAT_CAPTURED events
       - Test: Sound not called when soundEnabled=false
       - Test: Cleanup cancels in-progress animation

    3. Update GoatPiece.tsx:
       - Add optional props: `isFading?: boolean`, `isPlacing?: boolean`
       - Add CSS transition: `transition: 'cx 350ms ease-in-out, cy 350ms ease-in-out, opacity 200ms ease-out'`
       - When isFading: set opacity to 0
       - When isPlacing: use CSS @keyframes scale-in animation (scale 0 -> 1 over 200ms ease-out). Add the keyframe definition as a `<style>` in the SVG or use inline animation.
       - Use `cx` and `cy` attributes instead of `transform: translate()` for reliable CSS transitions (per research pitfall 2). This means changing from `<circle transform={translate(x,y)} r={9}>` to `<circle cx={x} cy={y} r={9}>`.

    4. Update TigerPiece.tsx:
       - Add optional prop: `isGlowing?: boolean`
       - Add CSS transition on transform: `transition: 'transform 350ms ease-in-out'` using inline style
       - For capture arc animation: use CSS @keyframes with a 3-point path (start -> apex above midpoint -> landing). Add an `arcAnimation` CSS class that can be applied when tiger is mid-capture.
       - When isGlowing: add pulsing glow effect (CSS animation: scale 1 -> 1.15 -> 1 over 500ms, add drop-shadow filter)
       - Keep using `transform={translate(x,y) rotate(45)}` but apply transition via style prop
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useAnimationQueue.test.ts</automated>
  </verify>
  <done>
    - useAnimationQueue processes GameEvents sequentially with correct timing
    - Piece components accept animation-related props and have CSS transitions
    - Sound triggers fire at animation start
    - Chain-hop events play sequentially with escalating capture sounds
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire animation queue into Board and GameScreen</name>
  <files>
    src/components/Board/Board.tsx
    src/components/GameScreen/GameScreen.tsx
    src/components/GameScreen/GameOverOverlay.tsx
  </files>
  <action>
    1. Update Board.tsx:
       - Add new props: `animationState?: AnimationState` (from useAnimationQueue)
       - Pass `isFading` prop to GoatPiece when `animationState?.fadingGoat === nodeId`
       - Pass `isPlacing` prop to GoatPiece when `animationState?.placingGoat === nodeId`
       - Pass `isGlowing` prop to TigerPiece when `animationState?.gameOverGlow === 'tiger-wins'`
       - When a goat needs glow (gameOverGlow === 'goat-wins'), pass `isGlowing` to GoatPiece (add isGlowing prop to GoatPiece if not done in Task 1)
       - For piece position overrides during animation: if animationState?.animatingPieces has an entry for a nodeId, use the animation target coordinates instead of the static NODES coordinates. This creates the smooth slide effect as CSS transitions interpolate from old to new position.
       - When `animationState?.isAnimating` is true, pass `disabled` or ignore `onNodeTap` (set pointer-events: none on the pieces/nodes layer, or have GameScreen not pass onNodeTap)

    2. Update GameScreen.tsx (GameBoard inner component):
       - Import and call `useAnimationQueue(game.lastEvents, soundEnabled, theme)` where soundEnabled and theme come from `useSettings()`
       - Pass `animationState` to Board component
       - When `animationState.isAnimating` is true, disable onNodeTap by passing a no-op, disable Undo/Redo buttons, hide End Turn button
       - This follows the same pattern as `isAIThinking` -- both gate user input
       - Combine the two flags: `const inputDisabled = isAIThinking || animationState.isAnimating`

    3. Update GameOverOverlay.tsx:
       - Add a brief delay before showing the overlay (the animation queue handles the 500ms glow, so the overlay should only appear after isAnimating becomes false)
       - The GameBoard component already conditionally renders GameOverOverlay when `status !== 'ongoing'`. Add a secondary gate: only show when `!animationState.isAnimating` as well. This naturally delays the overlay until the glow animation completes.

    4. Add CSS for capture arc animation to src/index.css (or as a <style> element in Board SVG):
       - Define @keyframes capture-arc for tiger jump: the keyframe needs to be dynamically positioned, so use a CSS class that triggers the arc via transform manipulation. Since exact start/end varies, the animation queue sets intermediate positions and CSS transitions handle the interpolation. The arc effect can be approximated by setting a midpoint y-offset (lift by 30px at 50% of the transition).
       - Alternative simpler approach: use two sequential position changes -- first move tiger to midpoint-above (175ms), then to landing (175ms). This creates a visual arc without @keyframes.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    - Piece slides animate smoothly between positions
    - Capture animation shows tiger arc and goat fade
    - Chain-hop captures play sequentially with pause between
    - Goat placement has entrance animation
    - User input is blocked during all animations
    - Game over glow plays before overlay appears
    - Sound effects fire during animations
    - All existing + new tests pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` -- full suite passes
- `npm run build` -- production build succeeds
- Visual: piece slide from node A to B shows smooth 350ms transition (not teleport)
- Visual: tiger capture shows arc motion over goat, goat fades out
- Visual: chain-hop shows sequential hops with visible pause
- Visual: goat placement has scale-in entrance effect
- Visual: game over shows winning piece glow for 500ms, then overlay
- Audio: sounds play in sync with visual animations
- Input: tapping during animation does nothing
</verification>

<success_criteria>
- Every move produces a visible animation matching the locked decisions from CONTEXT.md
- Chain-hop captures are deliberate and sequential, not simultaneous
- No animation-state desync (no teleporting pieces, no duplicate pieces)
- User input correctly gated during all animation types
</success_criteria>

<output>
After completion, create `.planning/phases/03-experience/03-02-SUMMARY.md`
</output>
