---
phase: 03-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - index.html
  - src/theme/theme.ts
  - src/hooks/useSettings.ts
  - src/hooks/useSettings.test.ts
  - src/audio/AudioEngine.ts
  - src/audio/sounds.ts
  - src/audio/AudioEngine.test.ts
  - src/theme/theme.test.ts
  - src/components/Settings/SettingsDropdown.tsx
  - src/components/Board/GoatPiece.tsx
  - src/components/Board/TigerPiece.tsx
  - src/components/Board/Board.tsx
  - src/components/Board/BoardNode.tsx
  - src/components/Board/BoardEdge.tsx
  - src/components/GameScreen/GameScreen.tsx
  - src/components/GameScreen/GameOverOverlay.tsx
autonomous: true
requirements:
  - POL-05
  - POL-06
  - POL-07
  - POL-08
  - POL-09

must_haves:
  truths:
    - "Switching theme changes all board and piece colors instantly without page reload"
    - "Sound toggle turns game audio on and off"
    - "Theme and sound preferences survive page refresh"
    - "No flash of wrong theme on page load"
    - "Sound effects play for place, slide, capture, win/loss, and illegal move events"
    - "Traditional theme sounds are stone/bell tones; Modern theme sounds are wood/chime tones"
    - "Settings gear opens a dropdown with theme toggle and sound toggle"
  artifacts:
    - path: "src/index.css"
      provides: "CSS custom property definitions for both themes"
      contains: "data-theme"
    - path: "src/theme/theme.ts"
      provides: "Theme type definitions and constants"
      exports: ["ThemeName", "THEMES"]
    - path: "src/hooks/useSettings.ts"
      provides: "Settings state management with localStorage persistence"
      exports: ["useSettings", "SettingsProvider", "Settings"]
    - path: "src/audio/AudioEngine.ts"
      provides: "Web Audio API synthesis engine"
      exports: ["audioEngine"]
    - path: "src/audio/sounds.ts"
      provides: "Sound recipes for each game event type per theme"
      exports: ["playPlace", "playSlide", "playCapture", "playWin", "playLoss", "playIllegal"]
    - path: "src/components/Settings/SettingsDropdown.tsx"
      provides: "Gear icon settings UI"
      exports: ["SettingsDropdown"]
  key_links:
    - from: "src/hooks/useSettings.ts"
      to: "localStorage"
      via: "getItem/setItem on mount and change"
      pattern: "localStorage\\.(get|set)Item"
    - from: "src/components/Settings/SettingsDropdown.tsx"
      to: "src/hooks/useSettings.ts"
      via: "useSettings hook"
      pattern: "useSettings"
    - from: "index.html"
      to: "localStorage"
      via: "inline script reading theme before React"
      pattern: "data-theme"
    - from: "src/components/Board/GoatPiece.tsx"
      to: "src/index.css"
      via: "CSS custom properties var()"
      pattern: "var\\(--goat"
---

<objective>
Establish the visual theming system (Traditional and Modern), Web Audio API sound engine, and settings persistence for Phase 3.

Purpose: All subsequent plans (animations, tutorial, history) depend on CSS custom properties for colors and the settings infrastructure for user preferences. This plan converts hardcoded colors to theme-aware CSS variables, builds the sound synthesis engine, and creates the settings UI.

Output: Two complete visual themes switchable via settings dropdown, a fully functional audio engine with distinct sound packs per theme, and localStorage-persisted user preferences.
</objective>

<execution_context>
@/Users/rk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-experience/03-CONTEXT.md
@.planning/phases/03-experience/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/engine/types.ts:
```typescript
export type GameEvent =
  | { type: 'PIECE_MOVED'; from: number; to: number; piece: Piece }
  | { type: 'GOAT_CAPTURED'; over: number; landedAt: number }
  | { type: 'GOAT_PLACED'; at: number }
  | { type: 'PHASE_CHANGED'; newPhase: Phase }
  | { type: 'GAME_OVER'; status: GameStatus }
  | { type: 'CHAIN_JUMP_AVAILABLE'; tigerAt: number }
  | { type: 'CHAIN_JUMP_ENDED'; tigerAt: number };
```

From src/components/Board/GoatPiece.tsx (current -- will be modified):
```typescript
// Currently uses hardcoded fills: fill="#d4a76a", stroke="#92400e" / "#f59e0b"
export function GoatPiece({ x, y, isSelected }: GoatPieceProps)
```

From src/components/Board/TigerPiece.tsx (current -- will be modified):
```typescript
// Currently uses hardcoded fills: fill="#dc2626", stroke="#991b1b" / "#f59e0b"
export function TigerPiece({ x, y, isSelected }: TigerPieceProps)
```

From src/components/Board/BoardNode.tsx (current -- 44px hit area, hardcoded fill):
```typescript
export function BoardNode({ node, isSelected, isLegalMove, onClick }: BoardNodeProps)
```

From src/components/Board/BoardEdge.tsx:
```typescript
// Renders line between two nodes
```

From src/components/GameScreen/GameScreen.tsx:
```typescript
// GameBoard inner component renders: TurnIndicator, PoolCounter, Board, buttons, GameOverOverlay
// bg-stone-900 is the hardcoded background
export function GameScreen({ aiConfig, onBackToMenu }: GameScreenProps)
```

From src/hooks/useGame.ts:
```typescript
export function useGame(): {
  gameState, selectedNode, legalMoves, status, canUndo, canRedo, lastEvents,
  onNodeTap, onEndChain, onUndo, onRedo, onNewGame
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Theme system, settings hook, and settings persistence</name>
  <files>
    src/index.css
    index.html
    src/theme/theme.ts
    src/hooks/useSettings.ts
    src/hooks/useSettings.test.ts
    src/theme/theme.test.ts
  </files>
  <behavior>
    - useSettings returns { theme, soundEnabled, setTheme, setSoundEnabled }
    - useSettings reads initial values from localStorage on mount (default: theme='traditional', soundEnabled=true)
    - setTheme('modern') updates localStorage and sets document.documentElement.dataset.theme = 'modern'
    - setSoundEnabled(false) updates localStorage
    - CSS custom properties change when data-theme attribute changes
    - :root/[data-theme="traditional"] defines warm stone colors; [data-theme="modern"] defines dark cool colors
    - index.html inline script sets data-theme from localStorage before React mounts (no flicker)
  </behavior>
  <action>
    1. Create src/theme/theme.ts:
       - Export `type ThemeName = 'traditional' | 'modern'`
       - Export `interface Settings { theme: ThemeName; soundEnabled: boolean }`
       - Export `const DEFAULT_SETTINGS: Settings = { theme: 'traditional', soundEnabled: true }`
       - Export `const SETTINGS_KEY = 'pulijoodam_settings'`

    2. Create src/hooks/useSettings.ts:
       - React context + provider pattern (SettingsProvider wraps app)
       - `useSettings()` hook returns `{ theme, soundEnabled, setTheme, setSoundEnabled }`
       - On mount: read from localStorage (wrapped in try/catch), fall back to defaults
       - On theme change: update localStorage AND set `document.documentElement.dataset.theme`
       - On soundEnabled change: update localStorage
       - Export SettingsProvider, useSettings, Settings type

    3. Update src/index.css:
       - Keep `@import "tailwindcss"` at top
       - Add CSS custom properties block for both themes per research (03-RESEARCH.md CSS Theme section):
         - `:root, [data-theme="traditional"]` with warm stone/earth board, brass/gold tigers, dark stone goats, sandstone bg, terracotta/copper accents
         - `[data-theme="modern"]` with dark charcoal board (#1a1a2e), amber/orange tigers, cyan/blue goats, near-black bg, purple accents
       - Include: --board-bg, --board-line, --board-line-width, --node-fill, --node-selected, --legal-move-stroke, --tiger-fill, --tiger-stroke, --goat-fill, --goat-stroke, --bg-primary, --bg-secondary, --text-primary, --text-secondary, --accent, --accent-hover, --select-stroke

    4. Update index.html:
       - Add inline `<script>` in `<head>` BEFORE the module script to prevent theme flicker:
         ```
         try { const s = JSON.parse(localStorage.getItem('pulijoodam_settings'));
         if (s && s.theme) document.documentElement.dataset.theme = s.theme; }
         catch(e) {}
         ```

    5. Write tests:
       - src/hooks/useSettings.test.ts: test localStorage read/write, defaults, theme/sound toggle
       - src/theme/theme.test.ts: test DEFAULT_SETTINGS values, SETTINGS_KEY constant
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useSettings.test.ts src/theme/theme.test.ts</automated>
  </verify>
  <done>
    - useSettings hook reads/writes localStorage correctly
    - CSS custom properties defined for both themes
    - index.html prevents theme flicker with inline script
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Audio engine, settings UI, and visual integration</name>
  <files>
    src/audio/AudioEngine.ts
    src/audio/sounds.ts
    src/audio/AudioEngine.test.ts
    src/components/Settings/SettingsDropdown.tsx
    src/components/Board/GoatPiece.tsx
    src/components/Board/TigerPiece.tsx
    src/components/Board/Board.tsx
    src/components/Board/BoardNode.tsx
    src/components/Board/BoardEdge.tsx
    src/components/GameScreen/GameScreen.tsx
    src/components/GameScreen/GameOverOverlay.tsx
    src/main.tsx
  </files>
  <behavior>
    - AudioEngine.playPlace('traditional') produces a stone-tap sound via OscillatorNode
    - AudioEngine.playPlace('modern') produces a wood-clack sound
    - AudioEngine.playCapture('traditional', 0) plays base capture; chainIndex=2 plays at higher pitch
    - AudioEngine lazily creates AudioContext on first sound call, resumes if suspended
    - SettingsDropdown renders gear icon, clicking opens dropdown with theme switch and sound toggle
    - All piece components use var(--tiger-fill), var(--goat-fill) etc. instead of hardcoded hex colors
    - GameScreen background uses var(--bg-primary) instead of bg-stone-900
    - Board SVG elements use CSS custom properties for all colors
  </behavior>
  <action>
    1. Create src/audio/AudioEngine.ts:
       - Singleton class with lazy AudioContext creation (on first sound method call)
       - Always call ctx.resume() before playing (autoplay policy)
       - Methods: playPlace, playSlide, playCapture, playWin, playLoss, playIllegal
       - Each method takes theme parameter ('traditional' | 'modern')
       - playCapture also takes chainIndex (default 0) for escalating pitch per chain-hop
       - Export singleton: `export const audioEngine = new AudioEngine()`

    2. Create src/audio/sounds.ts:
       - Sound recipe functions using OscillatorNode + GainNode + noise buffers
       - Traditional sounds: stone tap (sine, fast decay), grinding scrape (noise + bandpass), heavy impact (low sine burst), bell/gong (FM synthesis), low rumble (noise)
       - Modern sounds: wooden clack (triangle wave), soft scrape (filtered noise), sharp knock (square pulse), warm chime (sine harmonics), dull thud (sine + noise)
       - Chain-hop escalation: each successive chainIndex multiplies frequency by (1 + 0.15 * chainIndex) and volume by (1 + 0.1 * chainIndex)
       - Import and use these recipes from AudioEngine methods

    3. Create src/audio/AudioEngine.test.ts:
       - Mock AudioContext, OscillatorNode, GainNode with vi.fn()
       - Test: AudioContext created lazily (not on import)
       - Test: resume() called when context suspended
       - Test: each sound method creates oscillator nodes and connects to destination
       - Test: chain-hop escalation increases frequency

    4. Create src/components/Settings/SettingsDropdown.tsx:
       - Gear icon button (SVG cog) positioned in top-right via parent layout
       - Click toggles dropdown visibility
       - Dropdown contains: Theme label with "Traditional / Modern" toggle switch, Sound label with on/off toggle
       - Uses useSettings() hook to read/write preferences
       - Click outside closes dropdown
       - Styled with Tailwind + CSS custom properties for theme-awareness

    5. Update piece components to use CSS custom properties:
       - GoatPiece.tsx: fill={undefined}, use style={{ fill: 'var(--goat-fill)' }} or inline the var(). stroke uses var(--goat-stroke) normally and var(--select-stroke) when selected
       - TigerPiece.tsx: same pattern with var(--tiger-fill), var(--tiger-stroke), var(--select-stroke)
       - BoardNode.tsx: node circle fill uses var(--node-fill), selected uses var(--node-selected), legal-move ring uses var(--legal-move-stroke)
       - BoardEdge.tsx: stroke uses var(--board-line), strokeWidth uses var(--board-line-width)
       - Board.tsx: no color changes needed (colors are in child components), but if the SVG background rect exists, use var(--board-bg)

    6. Update GameScreen.tsx:
       - Replace `bg-stone-900` with inline style `backgroundColor: 'var(--bg-primary)'`
       - Add SettingsDropdown to the top bar area (next to the back-to-menu button)
       - Replace hardcoded text colors (text-stone-400, text-white, etc.) with CSS custom property references where appropriate
       - Update button styles to use theme-aware colors

    7. Update GameOverOverlay.tsx:
       - Replace hardcoded bg-stone-800 and text colors with CSS custom property references

    8. Update src/main.tsx:
       - Wrap `<App />` with `<SettingsProvider>` from useSettings
  </action>
  <verify>
    <automated>npx vitest run src/audio/AudioEngine.test.ts && npx vitest run</automated>
  </verify>
  <done>
    - All board elements use CSS custom properties (no hardcoded color hex values in piece/board components)
    - AudioEngine produces sounds for all 6 event types with theme-specific tones
    - Chain-hop capture sounds escalate in pitch
    - SettingsDropdown renders and toggles theme/sound
    - GameScreen, GameOverOverlay use theme-aware colors
    - SettingsProvider wraps the app
    - Full test suite passes (existing + new tests)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (existing 114 + new settings/audio tests)
- `npm run build` -- production build succeeds
- Visual check: switching theme in settings changes all colors instantly
- Visual check: no flash of wrong theme on page reload
- Audio check: placing a goat plays a sound, captures play a sound, win plays a sound
</verification>

<success_criteria>
- Two complete visual themes switchable without reload
- Sound effects play for all game events with theme-specific tones
- Settings persist in localStorage across sessions
- No theme flicker on load
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-experience/03-01-SUMMARY.md`
</output>
