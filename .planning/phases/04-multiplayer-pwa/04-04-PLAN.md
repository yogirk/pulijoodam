---
phase: 04-multiplayer-pwa
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SetupScreen/SetupScreen.test.tsx
autonomous: true
gap_closure: true
requirements:
  - MP-01
  - MP-02
  - MP-03
  - MP-04
  - MP-05
  - MP-06
  - MP-07
  - MP-08
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
  - PROD-06
  - PROD-07
  - PROD-08
  - PROD-09
  - PROD-10

must_haves:
  truths:
    - "All 228+ tests pass including 7 SetupScreen tests"
    - "SetupScreen test assertions match the component's actual selected-state styling"
  artifacts:
    - path: "src/components/SetupScreen/SetupScreen.test.tsx"
      provides: "SetupScreen unit tests with corrected selected-state assertions"
      contains: "scale-105"
  key_links: []
---

<objective>
Fix 3 failing SetupScreen tests whose assertions reference a stale `bg-amber-600` className that was replaced with shadow/scale styling during Phase 3.

Purpose: Close the single verification gap from 04-VERIFICATION.md (truth #21: "All tests pass"). The component uses `scale-105` (role buttons) and `scale-[1.02]` (difficulty buttons) as selected-state class indicators, plus inline `backgroundColor: var(--accent)` via style attribute. Tests must assert the current class names.
Output: All 228+ tests passing green.
</objective>

<execution_context>
@/Users/rk/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rk/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-multiplayer-pwa/04-VERIFICATION.md
@src/components/SetupScreen/SetupScreen.tsx
@src/components/SetupScreen/SetupScreen.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix SetupScreen test assertions to match current styling</name>
  <files>src/components/SetupScreen/SetupScreen.test.tsx</files>
  <action>
Update the 3 failing test assertions in SetupScreen.test.tsx. The component no longer uses Tailwind `bg-amber-600` for selected state. Instead it uses:
- Role buttons (role-goat, role-tiger): `scale-105` in className when selected, `opacity-70` when unselected
- Difficulty buttons: `scale-[1.02]` in className when selected, `opacity-70` when unselected

Replace each `bg-amber-600` assertion with the appropriate scale class:

1. **Test "defaults to Goat role and Medium difficulty"** (lines 17-19):
   - `expect(goatBtn.className).toContain('bg-amber-600')` -> `expect(goatBtn.className).toContain('scale-105')`
   - `expect(mediumBtn.className).toContain('bg-amber-600')` -> `expect(mediumBtn.className).toContain('scale-[1.02]')`

2. **Test "switches role when tiger is clicked"** (lines 30-31):
   - `expect(tigerBtn.className).toContain('bg-amber-600')` -> `expect(tigerBtn.className).toContain('scale-105')`
   - `expect(goatBtn.className).not.toContain('bg-amber-600')` -> `expect(goatBtn.className).toContain('opacity-70')`

3. **Test "switches difficulty when clicked"** (lines 72-75):
   - `expect(expertBtn.className).toContain('bg-amber-600')` -> `expect(expertBtn.className).toContain('scale-[1.02]')`
   - `expect(mediumBtn.className).not.toContain('bg-amber-600')` -> `expect(mediumBtn.className).toContain('opacity-70')`

Do NOT change any other tests. Only update the 6 specific assertions listed above.
  </action>
  <verify>
    <automated>npx vitest run src/components/SetupScreen/SetupScreen.test.tsx</automated>
  </verify>
  <done>All 7 SetupScreen tests pass. Full test suite (npx vitest run) reports 0 failures.</done>
</task>

</tasks>

<verification>
Run full test suite: `npx vitest run`
Expected: 228+ tests pass, 0 failures.
</verification>

<success_criteria>
- All 7 SetupScreen tests pass
- Full test suite passes with 0 failures
- No test logic changed -- only className assertions updated to match current component styling
</success_criteria>

<output>
After completion, create `.planning/phases/04-multiplayer-pwa/04-04-SUMMARY.md`
</output>
